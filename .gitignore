node_modules
.next
out
.env*
!.env.example
.DS_Store
*.log
.env.local

# Local AI-tooling config/skills — not part of the app, kept out of the
# public repo since they can carry local file paths and personal workflow
# details.
.claude/
.agents/

tsconfig.tsbuildinfo

# Schema/RLS policy history — some past migrations document real fixed
# vulnerabilities (see 004, 006). Keeping the full policy surface private
# reduces the chance of the same class of gap being probed for on client
# data, even though current tables hold no client data themselves.
/supabase/migrations/

# Local-only OG card editor: dev tooling that drives a headless browser to
# write files into public/. Never intended to ship.
/app/og-lab/
/app/api/og-draft/
.og-lab-draft.html
